class TransactionHistoryModel {
  final String title, subTitle, mount;
  final bool isWithdrawal;

 const TransactionHistoryModel({required this.title, required this.subTitle, required this.mount, required this.isWithdrawal});
}
